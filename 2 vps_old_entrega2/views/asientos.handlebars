<h1>Asientos</h1>
<div class="ml-5 mr-5">
	{{! Iteramos el array de asientos }}
	{{#each asientos}}
		<div class="form-check form-check-inline">
			{{! Si el asiento esta vendido, dejamos el checkbox deshabilitado, pues no se debe poder comprar }}
			{{#if this.vendido}}
				<input
					class="form-check-input"
					type="checkbox"
					id="inlineCheckbox1"
					value="option1"
					disabled
				/>
				{{! Si no esta vendido, entonces se deja habilitado }}
			{{else}}
				<input
					class="form-check-input"
					type="checkbox"
					id="inlineCheckbox1"
					value="option1"
				/>
			{{/if}}
			<label
				class="form-check-label"
				for="inlineCheckbox1"
			>{{this.numero}}</label>
		</div>
	{{/each}}
</div>